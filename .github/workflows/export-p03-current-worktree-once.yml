name: Export P03 current worktree once

on:
  push:
    branches: [agent/p03-current-worktree-20260719]
    paths:
      - ".github/workflows/export-p03-current-worktree-once.yml"
  pull_request:
    types: [opened, synchronize, reopened]
    branches: [main]

permissions:
  contents: read

jobs:
  export:
    if: github.event_name == 'push' || github.head_ref == 'agent/p03-current-worktree-20260719'
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v4
      - uses: actions/upload-artifact@v4
        with:
          name: liaison-p03-current-worktree
          include-hidden-files: true
          retention-days: 1
          path: |
            .
            !.git/**
