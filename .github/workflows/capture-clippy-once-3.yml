name: Capture next Clippy diagnostic

on:
  push:
    branches:
      - agent/r1-rust-ddd-cli-v3
    paths:
      - ".github/workflows/capture-clippy-once-3.yml"

permissions:
  contents: write

jobs:
  capture:
    runs-on: ubuntu-24.04
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v4
        with:
          ref: agent/r1-rust-ddd-cli-v3
          fetch-depth: 0
      - run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          mkdir -p .diagnostics
          cargo clippy --workspace --all-targets --all-features --locked -- -D warnings > .diagnostics/clippy.txt 2>&1 || true
          rm .github/workflows/capture-clippy-once-3.yml
          git add -A
          git commit -m "chore: capture next Rust lint diagnostic"
          git push origin HEAD:agent/r1-rust-ddd-cli-v3
