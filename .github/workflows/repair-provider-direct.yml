name: Repair direct provider sources

on:
  push:
    branches:
      - agent/r1-provider-sdk-v4
    paths:
      - ".bootstrap/provider-repair/READY"
  pull_request:
    branches:
      - agent/r1-rust-ddd-cli-v3
    types: [opened, synchronize, reopened]
    paths:
      - ".bootstrap/provider-repair/READY"

permissions:
  contents: write

concurrency:
  group: repair-direct-provider-sources
  cancel-in-progress: false

jobs:
  repair:
    if: github.event_name == 'push' || github.head_ref == 'agent/r1-provider-sdk-v4'
    runs-on: ubuntu-latest
    timeout-minutes: 45
    steps:
      - name: Check out provider branch
        uses: actions/checkout@v4
        with:
          ref: agent/r1-provider-sdk-v4
          fetch-depth: 0

      - name: Install reviewed direct sources
        shell: bash
        run: |
          set -euo pipefail
          echo "4720e98ccf2bf7ecf415407c62880c35eae46c885002085104e6fd76b7fd5874  .bootstrap/provider-repair/direct-sources.tar.gz.b64" | sha256sum -c -
          base64 --decode .bootstrap/provider-repair/direct-sources.tar.gz.b64 > /tmp/provider-direct-repair.tar.gz
          echo "d00de80134f678f27b376ce51e4e86862194de3b7d3aab3efa9bff04be9e23a0  /tmp/provider-direct-repair.tar.gz" | sha256sum -c -
          tar -xzf /tmp/provider-direct-repair.tar.gz -C "$GITHUB_WORKSPACE"

      - name: Install pinned Rust toolchain
        shell: bash
        run: |
          set -euo pipefail
          rustup toolchain install 1.97.1 --profile minimal --component rustfmt --component clippy
          rustup default 1.97.1
          rustc --version --verbose
          cargo --version --verbose

      - name: Format and regenerate lockfile
        shell: bash
        run: |
          set -euo pipefail
          cargo generate-lockfile
          cargo fmt --all
          cargo fmt --all --check

      - name: Validate repository and contracts
        shell: bash
        run: |
          set -euo pipefail
          python scripts/check_repository.py
          python scripts/check_spec.py
          python scripts/check_architecture.py
          python scripts/check_providers.py
          python scripts/check_wit_contract.py

      - name: Check, lint, and test
        shell: bash
        run: |
          set -euo pipefail
          cargo check --workspace --all-targets --all-features --locked
          cargo clippy --workspace --all-targets --all-features --locked -- -D warnings
          cargo test --workspace --all-features --locked

      - name: Publish clean direct sources
        shell: bash
        run: |
          set -euo pipefail
          rm -rf .bootstrap/provider-payload .bootstrap/provider-repair
          rm -f .github/workflows/repair-provider-direct.yml
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git commit -m "fix: complete provider conformance implementation"
          git push origin HEAD:agent/r1-provider-sdk-v4
