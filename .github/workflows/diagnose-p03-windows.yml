name: Diagnose P03 Windows tests once

on:
  pull_request:
    branches: [main]
    paths:
      - ".github/workflows/diagnose-p03-windows.yml"
      - "adapters/vault-markdown/tests/windows_directory_sync_probe.rs"

permissions:
  contents: read

jobs:
  diagnose:
    if: github.head_ref == 'fix/p03-post-merge-hardening'
    runs-on: windows-2025
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v4
      - name: Capture capability-directory sync probe
        shell: pwsh
        run: |
          New-Item -ItemType Directory -Force -Path diagnostics | Out-Null
          cargo test -p liaison-vault-markdown --test windows_directory_sync_probe --all-features --locked -- --nocapture *> diagnostics/p03-windows-tests.txt
          $status = $LASTEXITCODE
          Set-Content -Path diagnostics/status.txt -Value $status
          exit 0
      - uses: actions/upload-artifact@v4
        with:
          name: p03-windows-test-diagnostics
          path: diagnostics
          if-no-files-found: error
          retention-days: 1
