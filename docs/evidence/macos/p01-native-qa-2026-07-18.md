# P01 native macOS QA — 2026-07-18

Status: **P01 vertical slice passed on Apple Silicon; release gates remain closed**

## Artifact identity

- source commit: `5f06b464bd644209020cdd3b1d9e302f92db933f`;
- build command: `cargo tauri build --bundles app`;
- bundle: local review output, not installed as a supported application;
- executable architecture: Apple Silicon;
- executable SHA-256: `1574d7edc1fd355f60795a854628117bfbbddf85c1824308292da64b569079d3`;
- `Info.plist` SHA-256: `d83c9f6df66e37ef7c0d8cc3abab11b96fd6c88f2a72322e870d01f2fed15d9e`;
- bundle version: `0.1.0-alpha.1`;
- bundle identifier: `io.github.electric-town.liaison-rm`;
- signing: `codesign --verify --deep --strict` did not pass; this is not a distributable artifact.

The source checkout was clean at the recorded commit before the build. This is exact-commit native workflow evidence for P01, not macOS release provenance.

## Native workflow results

Synthetic data was created in a new temporary workspace through the compiled Tauri application.

| Check | Result | Evidence |
|---|---|---|
| Launch exact local bundle | Pass | The native window opened at `tauri://localhost`. |
| Runtime version | Pass | Visible status reported `0.1.0-alpha.1`, matching bundle metadata. |
| Authority copy | Pass | The interface said `local-authoritative review build` and `no connection configured`; it did not claim Airgap proof. |
| Workplace default | Pass | Workplace was the selected profile before creation. |
| Workspace creation | Pass | A readable schema-version-1 workspace manifest was created. |
| Build profile | Pass | The manifest recorded `connected-local`. |
| People warning state | Pass | The pre-workspace warning was removed from the visual and accessibility tree after creation. |
| Native Person request | Pass | The UI and Tauri adapter accepted the same session-bound request shape. |
| Person persistence | Pass | The application reported success and displayed the synthetic Person at revision 1. |
| Independent file inspection | Pass | A readable `liaison-person` Markdown/YAML record contained the synthetic name and `example.test` address. |
| Workspace Health | Pass, narrow | The compiled application reported the created workspace as valid. |
| Quit and relaunch | Pass | The exact local bundle reopened after an ordinary quit. |
| Selected-workspace restoration | Not implemented | The application relaunched with no workspace selected. |
| Manual reopen after this relaunch | Not completed | Desktop control detected concurrent user interaction during the final selection step; no claim is made. |
| Native network denial | Not proven | No process-level network capture was bound to this checksum. |

## Defects found and fixed during native QA

The pre-P01 installed bundle failed Person creation at the Tauri argument boundary. After that contract was corrected, compiled WebKit exposed another problem that the Chromium fixture did not: the asynchronous submit handler read `event.currentTarget` after awaiting the native command. WebKit cleared the event target, so the Person file was written but the UI reported failure and invited a dangerous retry.

Commit `5f06b464bd644209020cdd3b1d9e302f92db933f` captures the form before the await, supplies a keyboard-submit fallback, and tests Enter submission. It also restores the platform meaning of the HTML `hidden` attribute so the pre-workspace warning is not announced after a workspace opens.

## Remaining gates

- exact-head Linux, Windows, macOS, and universal-bundle CI;
- signed/notarised DMG and clean-machine install assessment;
- manual reopen evidence on the same artifact;
- persistence and lock behavior after P02/P03;
- process-level offline and undeclared-egress evidence;
- compiled P04 design review, keyboard, VoiceOver, 200% zoom/reflow, high contrast, reduced motion, interruption recovery, and full WCAG 2.2/applicable EN 301 549 evidence;
- uninstall/reinstall proof that the user-selected workspace remains untouched.

P01 remains a pre-alpha application-foundation change. It does not close B0, P02, P03, Airgap, accessibility conformance, signing, notarisation, or distribution.
