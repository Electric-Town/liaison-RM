# Knowledge articles

This directory contains task- and problem-oriented articles maintained through the repository’s KCS-informed workflow.

Use the structure defined in `docs/standards/knowledge-centered-work.md`. Search before creating a new article, update an article when new evidence changes the resolution, and retain retired articles with a pointer to their replacement.

Article identifiers use `KCS-NNNN`. Identifiers are stable even when titles change.
