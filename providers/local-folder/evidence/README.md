# Evidence pending

This directory will contain exact-commit conformance evidence after the provider matrix passes on Ubuntu, macOS, and Windows. Until then, `descriptor.json` remains `not-tested` and the provider is not release-ready.
